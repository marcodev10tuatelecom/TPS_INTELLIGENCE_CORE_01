# V0002 — Least-Privilege Role and Grant Baseline

Status: DESIGNED / NOT EXECUTED.

Prerequisites: V0001 PASS, CORE-02 approved identity model, object names/owner proven, application/service identities identified, privilege-negative tests prepared.

Creates only TPS application roles and grants explicit object/package permissions. Does not create users, passwords or credentials. Unified audit policy remains a separately reviewed subchange because actual Autonomous audit privileges/naming must be proven first.
